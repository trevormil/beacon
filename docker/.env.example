# Copy to .env in this directory; never commit the real file.
ADMIN_TOKEN=replace-me-with-openssl-rand-hex-32

# Postgres profile
POSTGRES_PASSWORD=changeme
POSTGRES_PORT=5432

# MySQL profile
MYSQL_PASSWORD=changeme
MYSQL_ROOT_PASSWORD=rootchangeme
MYSQL_PORT=3306

# Mongo profile
MONGO_PASSWORD=changeme
MONGO_PORT=27017

# Host port the Beacon API binds to
BEACON_PORT=4747
